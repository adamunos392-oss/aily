# [已迁移] 本件已拆分至 harness-core/specification/default/

原《后端分层开发规范》已按节迁入规范集：

- `specification/default/backend/layers.md`（头部 / 层 1 模型层 / 层 2 数据访问层 / 层 3 Service 层 / 易错点 Models-DB-Service）
- `specification/default/backend/api-design.md`（层 4 API 路由层 / 易错点 API）
- `specification/default/backend/error-handling.md`（层 2 错误处理链路）
- 「深入阅读」节已删除（指向 pycore/docs，与禁读条款矛盾）

执行时一律读取新路径。本目录为过渡期旧引用重定向占位，待删除。
