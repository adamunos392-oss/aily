# 前端 API 基址（相对路径，禁止写完整后端 URL）
VITE_API_BASE_URL=/api

# Mock 开关：true 走 frontend/src/mocks，false 走 Vite 代理后端（T-006 闭环验收为 false）
VITE_USE_MOCK=false

# Vite 开发代理目标（仅出现在代理配置，不进浏览器请求 URL）
VITE_BACKEND_PROXY_TARGET=http://localhost:8099

# Axios 超时（毫秒）
VITE_AXIOS_TIMEOUT_MS=10000
