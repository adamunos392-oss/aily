---
alwaysApply: true
description: SDD V7_2 Cursor 入口适配器。打开规范包后自动加载 harness-core/router.md 和 harness-core/protocols/development-rules.md。
---

# SDD V7_2 Cursor Adapter

你现在处在 SDD V7_2 Harness 工作区。Cursor 打开的目录是 Harness 根目录，但业务项目文件不得直接写在 Harness 根目录。

必须先读取并遵守：

1. `harness-core/router.md`

当用户进入产品设计、功能升级、Bugfix 或开发模式时，必须按 `harness-core/` 下的对应 command / skill / agent 规范执行。
